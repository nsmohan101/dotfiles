
# Tab accepts the autosuggestion (in insert mode)
# bind -M insert \t accept-autosuggestion

# Completion menu on Ctrl+Space (fallback Ctrl+I)
# bind -M insert ctrl-n '__fzf_complete'
# bind -M insert \ci complete
